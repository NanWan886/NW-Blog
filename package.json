{
  "name": "nw-blog",
  "version": "1.0.0",
  "description": "NW Blog",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev --local"
  },
  "keywords": ["cloudflare", "workers", "navigation", "statistics"],
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
